import React from "react";
//The complexity of the app not necessarily require context at the moment
//but it will surely require it in the future, as new nested components are being intrduced
//In c.s, We always plan ahead
export default React.createContext([]);
